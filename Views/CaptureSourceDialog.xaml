<Window x:Class="TS3ScreenShare.Views.CaptureSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Source"
        Width="680" Height="520"
        MinWidth="560" MinHeight="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Border Background="{StaticResource BgBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TITLEBAR -->
            <Border Grid.Row="0"
                    Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="Titlebar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal"
                                VerticalAlignment="Center" Margin="12,0">
                        <TextBlock Text="▶" Foreground="{StaticResource AccentBrush}"
                                   FontSize="11" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Select stream source" Foreground="{StaticResource TextBrush}"
                                   FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="✕"
                            Style="{StaticResource TitlebarBtnClose}"
                            Click="BtnCancel_Click"/>
                </Grid>
            </Border>

            <!-- CONTENT -->
            <Grid Grid.Row="1" Margin="20,16,20,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- MONITORS LABEL -->
                <TextBlock Grid.Row="0"
                           Text="// MONITORS"
                           Foreground="{StaticResource DimBrush}"
                           FontFamily="Consolas" FontSize="10"
                           Margin="0,0,0,10"/>

                <!-- MONITORS LIST -->
                <ItemsControl x:Name="MonitorList" Grid.Row="1"
                              Margin="0,0,0,16">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="MonitorCard"
                                    Width="140" Height="90"
                                    Background="{StaticResource SurfaceBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Margin="0,0,10,0"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="MonitorCard_Click">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Monitor icon area -->
                                    <Grid Grid.Row="0" Margin="12,10,12,6">
                                        <Border Background="{StaticResource BgBrush}"
                                                BorderBrush="{StaticResource DimBrush}"
                                                BorderThickness="1"
                                                CornerRadius="2">
                                            <TextBlock Text="{Binding Icon}"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       FontSize="18"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>

                                    <!-- Monitor name -->
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding Name}"
                                               Foreground="{StaticResource TextBrush}"
                                               FontSize="11"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"
                                               HorizontalAlignment="Center"
                                               Margin="6,0,6,8"/>
                                </Grid>
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" Opacity="0.3"
                                                      BlurRadius="8" ShadowDepth="2"/>
                                </Border.Effect>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="MonitorCard" Property="BorderBrush"
                                            Value="{StaticResource AccentBrush}"/>
                                    <Setter TargetName="MonitorCard" Property="BorderThickness"
                                            Value="2"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- WINDOWS LABEL + SEARCH -->
                <Grid Grid.Row="2" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="// WINDOWS"
                               Foreground="{StaticResource DimBrush}"
                               FontFamily="Consolas" FontSize="10"
                               VerticalAlignment="Center"/>
                    <Border Grid.Column="1"
                            Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1" CornerRadius="4">
                        <Grid>
                            <TextBlock Text="🔍"
                                       Foreground="{StaticResource DimBrush}"
                                       FontSize="11"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       IsHitTestVisible="False"/>
                            <TextBox x:Name="TxtSearch"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{StaticResource TextBrush}"
                                     FontFamily="Consolas" FontSize="11"
                                     CaretBrush="{StaticResource AccentBrush}"
                                     Padding="26,5,6,5"
                                     TextChanged="TxtSearch_TextChanged"/>
                        </Grid>
                    </Border>
                </Grid>

                <!-- WINDOWS LIST -->
                <Border Grid.Row="3"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1" CornerRadius="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="WindowList" Margin="4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="WinRow"
                                            Background="Transparent"
                                            CornerRadius="4"
                                            Padding="10,6"
                                            Margin="0,1"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="WindowRow_Click">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⬜"
                                                       Foreground="{StaticResource DimBrush}"
                                                       FontSize="11"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource TextBrush}"
                                                       FontSize="12"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter TargetName="WinRow" Property="Background"
                                                    Value="#1a2a3a"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="WinRow" Property="Background"
                                                    Value="#0d1a26"/>
                                        </Trigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- FOOTER -->
            <Border Grid.Row="2"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8"
                    Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="SelectedLabel"
                               Grid.Column="0"
                               Text="Nothing selected"
                               Foreground="{StaticResource DimBrush}"
                               FontFamily="Consolas" FontSize="11"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Cancel"
                            Width="90" Height="34"
                            Background="{StaticResource SurfaceBrush}"
                            Foreground="{StaticResource TextBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            FontSize="13" FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="BtnCancel_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bg"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bg" Property="Background" Value="#1e2a3a"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="BtnSelect"
                            Grid.Column="3"
                            Content="▶  Start"
                            Style="{StaticResource ConnectBtn}"
                            Width="110"
                            IsEnabled="False"
                            Click="BtnSelect_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
